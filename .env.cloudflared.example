# Copy to .env.cloudflared and replace with your Cloudflare named tunnel token.
# Create a token in Cloudflare Zero Trust:
#   Networks -> Tunnels -> <your tunnel> -> Configure -> Tunnel token

CLOUDFLARE_TUNNEL_TOKEN=replace-with-your-tunnel-token
