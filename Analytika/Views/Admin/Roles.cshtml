@model Analytika.Models.ViewModels.RoleListViewModel
@{
    ViewData["Title"] = "Role Management";
}

<div class="container-fluid py-4 px-4">

    <!-- Breadcrumbs & Page Header -->
    @await Html.PartialAsync("_PageHeader", new {
        Title = "Role Management",
        Subtitle = "Create, edit, and manage standard application roles and permissions",
        Crumbs = new[] { ("Admin", "/Admin/Users"), ("Roles", (string?)null) }
    })

    <div class="page-header mb-4">
        <div>
            <h5 class="mb-0 fw-bold text-navy">User Roles</h5>
        <div class="d-flex gap-2">
            <form asp-action="EnsureStandardRoles" method="post">
                @Html.AntiForgeryToken()
                <button type="submit" class="btn btn-sm btn-outline-secondary rounded-md">
                    <i class="fas fa-shield-alt me-1"></i>Ensure Standard Roles
                </button>
            </form>
            <a asp-action="Users" class="btn btn-sm btn-navy">
                <i class="fas fa-users me-1"></i>Users
            </a>
        </div>
    </div>

    @if (TempData["Success"] is string success)
    {
        <div class="alert alert-success border-0 shadow-sm rounded-md">
            <i class="fas fa-check-circle me-1"></i>@success
        </div>
    }
    @if (TempData["Error"] is string error)
    {
        <div class="alert alert-danger border-0 shadow-sm rounded-md">
            <i class="fas fa-exclamation-circle me-1"></i>@error
        </div>
    }

    <div class="row g-4">
        <div class="col-lg-4">
            <div class="card border-0 shadow-sm rounded-xl">
                <div class="card-body p-4">
                    <h6 class="fw-bold mb-3 text-navy">Create Role</h6>
                    <form asp-action="CreateRole" method="post">
                        @Html.AntiForgeryToken()
                        <label class="form-label small fw-semibold">Role Name</label>
                        <input name="newRoleName" class="form-control mb-3" maxlength="64" placeholder="e.g. ClaimsManager" />
                        <button type="submit" class="btn btn-primary w-100">
                            <i class="fas fa-plus me-1"></i>Create Role
                        </button>
                    </form>
                </div>
            </div>

            <div class="card border-0 shadow-sm mt-4 rounded-xl">
                <div class="card-body p-4">
                    <h6 class="fw-bold mb-3 text-navy">Standard Roles</h6>
                    <div class="d-flex flex-wrap gap-2">
                        @foreach (var role in Model.StandardRoles)
                        {
                            <span class="badge bg-sky-wash text-slate-700" style="border-radius:999px;padding:.45rem .65rem;">@role</span>
                        }
                    </div>
                    <p class="text-muted small mb-0 mt-3">
                        Admin and FacilityAdmin are protected because they control administration and access boundaries.
                    </p>
                </div>
            </div>
        </div>

        <div class="col-lg-8">
            <div class="card border-0 shadow-sm rounded-xl">
                <div class="card-body p-0">
                    <table class="table align-middle mb-0">
                        <thead>
                            <tr>
                                <th class="ps-4">Role</th>
                                <th>Type</th>
                                <th class="text-center">Users</th>
                                <th class="pe-4 text-end">Actions</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var role in Model.Roles)
                            {
                                <tr>
                                    <td class="ps-4">
                                        <form asp-action="EditRole" method="post" class="d-flex align-items-center gap-2">
                                            @Html.AntiForgeryToken()
                                            <input type="hidden" name="id" value="@role.Id" />
                                            <input name="roleName" value="@role.Name" class="form-control form-control-sm"
                                                   style="max-width:260px;" maxlength="64" @(role.IsProtected ? "readonly" : "") />
                                            <button type="submit" class="btn btn-sm btn-outline-secondary" title="Save role name"
                                                    @(role.IsProtected ? "disabled" : "")>
                                                <i class="fas fa-save"></i>
                                            </button>
                                        </form>
                                    </td>
                                    <td>
                                        @if (role.IsProtected)
                                        {
                                            <span class="badge text-teal" style="background:rgba(102,178,178,.14);">Protected</span>
                                        }
                                        else if (role.IsStandard)
                                        {
                                            <span class="badge text-cyan-light" style="background:rgba(0,139,139,.12);">Standard</span>
                                        }
                                        else
                                        {
                                            <span class="badge bg-sky-wash text-slate-700">Custom</span>
                                        }
                                    </td>
                                    <td class="text-center">
                                        <span class="fw-semibold text-navy">@role.UserCount</span>
                                    </td>
                                    <td class="pe-4 text-end">
                                        <form asp-action="DeleteRole" method="post" class="d-inline">
                                            @Html.AntiForgeryToken()
                                            <input type="hidden" name="id" value="@role.Id" />
                                            <button type="submit" class="btn btn-sm btn-outline-danger"
                                                    onclick="return confirm('Delete role @role.Name?')"
                                                    @(role.IsProtected || role.UserCount > 0 ? "disabled" : "")>
                                                <i class="fas fa-trash-alt"></i>
                                            </button>
                                        </form>
                                    </td>
                                </tr>
                            }
                            @if (!Model.Roles.Any())
                            {
                                <tr>
                                    <td colspan="4" class="text-center text-muted py-4">No roles found.</td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</div>
